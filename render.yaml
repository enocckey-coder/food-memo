services:
  - type: web
    name: food-memo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_cloud:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
